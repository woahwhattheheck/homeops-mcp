[project]
name = "homeops-relay"
version = "1.0.0"
description = "Evidence-first Alexa+ MCP server for household maintenance workflows"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}

[tool.pyright]
pythonVersion = "3.10"
