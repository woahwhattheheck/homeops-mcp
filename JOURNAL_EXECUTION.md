# HomeOps durable journal execution record

The real-process rehearsal is prepared in `homeops_relay/rehearse_journal.py`; execution is pending the coherent storage/server implementation. No process result is claimed in this checkpoint.

The planned accepted record will retain normal and real optimized runs, literal child-process commands and lifecycle outcomes, HTTP request/response bodies, CLI stdout/stderr/exits, source identities, original/restored/continued receipt and snapshot identities, and the distinction between event export and durable retry recovery. Existing raw evidence will be preserved if a run fails.
